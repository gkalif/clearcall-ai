fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
pydantic==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
aiofiles==23.2.1

# AI APIs
anthropic>=0.26.0         # Claude — transcript cleaning + summarization
openai>=1.30.0            # Whisper API (STT) + TTS

# Local Whisper STT (free, no API key needed)
# Uncomment to use STT_BACKEND=whisper_local
# openai-whisper==20231117
# torch  # pulled in by whisper automatically

# PostgreSQL (production)
# psycopg2-binary==2.9.9

# S3 storage (production)
# boto3==1.34.0
